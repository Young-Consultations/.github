# AI-SDLC 2.4.3 authentication repair

**Status:** repaired target and control plane published and attested; deployed
Runtime Preflight and REAL preflight remain pending.

The original 2.4.3 release selected the reviewed `consulting-playbook`
production-authentication repair after the 2.4.2 REAL attempt exposed a missing
authenticated provider request. REAL-preflight run `35640134916` then proved
that the published target still called the 2.4.2 receiver. After the target
repair merged, run `35654316863` passed immutable SIM and failed because the
control-plane registry still selected the old target commit. This repair does
not change the closed v2 payload schemas, source approval, activation,
draft-only publication boundary, or result contract. Published
`ai-sdlc-v2.4.2` remains the rollback baseline while deployed verification completes.

The owner explicitly authorized replacement of the defective 2.4.3 tags rather
than publication of 2.4.4. That is a limited risk exception to the normal
immutable-tag requirements. All evidence attached to the prior 2.4.3 tag
targets is invalid for acceptance and the path remains quarantined until the
replacement identities are attested and both deployed gates pass.

## Evidence and composition

- Protected target probe run
  [`35555329988`](https://github.com/Young-Consultations/consulting-playbook/actions/runs/35555329988)
  completed a provider response through the two-read authentication handoff.
- The replaced target tag `codex-adapter-v2.4.3` resolves to reviewed repair
  commit `050dc7bb4832eab77fca3e070d2ea1917d82e26e`.
- The target's committed `TC-MVP-CI-001` report digest is
  `9bb8927334741c80e61e54e722e5b07ef28476ad52108a32051183fe81416d38`.
- The published router and receiver self-pin to `ai-sdlc-v2.4.3`.
- Only `consulting-playbook` remains enabled; no activation state changes.
- The replaced control-plane tag `ai-sdlc-v2.4.3` resolves to reviewed commit
  `3da7ed9b7bf76d00ae35e4accc733ac8f95259c5`.
- `release/current-runtime.json` records the published and attested repaired
  composition.
- The source consumer already selects 2.4.3. That pin does not authorize a live
  approval while Runtime Preflight or REAL preflight fails.

## Required publication sequence

1. **Completed:** merge target repair PR #61 and replace
   `codex-adapter-v2.4.3` with reviewed commit
   `050dc7bb4832eab77fca3e070d2ea1917d82e26e`.
2. **Completed:** merge control-plane repair PR #73, binding the repaired target
   commit and report digest while clearing the obsolete publication attestation.
3. **Completed:** replace `ai-sdlc-v2.4.3` with exact reviewed PR #73 merge
   commit `3da7ed9b7bf76d00ae35e4accc733ac8f95259c5`.
4. **Completed by this attestation:** record the replacement tag commit, restore
   `tag_published: true`, regenerate the runtime record, and pass publishable
   release checks.
5. **Pending after attestation merge:** run deployed Runtime Preflight with
   `candidate_mode: false`, then run REAL preflight. Both must pass before any
   live approval.
6. Use a fresh harmless REAL implement issue. Portfolio issue #148 is terminal
   failed evidence and must not be reused.

## Required checks

Candidate checks:

```console
python3 scripts/generate_current_runtime.py --check
python3 scripts/runtime_preflight.py --offline --candidate
python3 scripts/validate_release.py
python3 scripts/verify_release_target_workflows.py \
  --repository Young-Consultations/consulting-playbook
python3 -m pytest
```

After publication attestation:

```console
python3 scripts/generate_current_runtime.py --check
python3 scripts/runtime_preflight.py --offline
python3 scripts/validate_release.py --require-publishable
python3 -m pytest
```

After this attestation merges, run the `AI-SDLC Runtime Preflight` workflow
with `candidate_mode: false`. The offline command validates the runtime shape
without remote or credential inspection; it does not satisfy the deployed gate.

The passing protected probe proves the repaired provider-authentication handoff.
It does not itself prove branch creation, validation, draft-PR publication,
result receipt, or source projection; those remain obligations of the fresh
end-to-end REAL verification.
