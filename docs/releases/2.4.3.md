# AI-SDLC 2.4.3 authentication repair

**Status:** target and control plane published and attested; deployed Runtime
Preflight and the source-consumer repin remain pending.

This patch selects the reviewed `consulting-playbook` production-authentication
repair after the 2.4.2 REAL attempt exposed a missing authenticated provider
request. It does not change the closed v2 payload schemas, source approval,
activation, draft-only publication boundary, or result contract. Published
`ai-sdlc-v2.4.2` remains the rollback baseline until this sequence completes.

## Evidence and composition

- Protected target probe run
  [`35555329988`](https://github.com/Young-Consultations/consulting-playbook/actions/runs/35555329988)
  completed a provider response through the two-read authentication handoff.
- The immutable target tag `codex-adapter-v2.4.3` resolves to reviewed commit
  `1a5da85a4e29b83ba72e1bf4354d7770035cd367`.
- The target's committed `TC-MVP-CI-001` report digest is
  `25be867dfc891222cc6fb49b3d9c0c708fee95834176ee464f87ae191f3ba44b`.
- The published router and receiver self-pin to `ai-sdlc-v2.4.3`.
- Only `consulting-playbook` remains enabled; no activation state changes.
- The immutable control-plane tag `ai-sdlc-v2.4.3` resolves to reviewed merge
  commit `9b240014d9fd5b5987cd8bfcde742d9b6079f501`.
- `release/current-runtime.json` records the published composition. The source
  consumer remains on 2.4.2 until deployed Runtime Preflight passes and its
  separate repin is reviewed and merged.

## Required publication sequence

1. **Completed:** review and merge the control-plane candidate after release
   validation, release-aware target verification, SIM, and candidate Runtime
   Preflight pass.
2. **Completed:** create immutable `ai-sdlc-v2.4.3` at reviewed merge commit
   `9b240014d9fd5b5987cd8bfcde742d9b6079f501`.
3. **Completed by this attestation:** record the resolved tag commit, set
   `tag_published` to `true`, regenerate the runtime record, and pass the
   publishable release checks.
4. **Pending after attestation merge:** run the deployed `AI-SDLC Runtime
   Preflight` workflow with `candidate_mode: false` and require remote tag,
   activation, and credential-metadata checks to pass.
5. Only after that deployed gate passes, merge the separate `portfolio-tasks`
   repin to `ai-sdlc-v2.4.3`.
6. Run SIM and live verify, then use a fresh harmless REAL implement issue.
   Portfolio issue #148 is terminal failed evidence and must not be reused.

## Required checks

Candidate checks:

```console
python3 scripts/generate_current_runtime.py --check
python3 scripts/runtime_preflight.py --offline --candidate
python3 scripts/validate_release.py
python3 scripts/verify_release_target_workflows.py \
  --repository Young-Consultations/consulting-playbook
python3 -m pytest
```

After publication attestation:

```console
python3 scripts/generate_current_runtime.py --check
python3 scripts/runtime_preflight.py --offline
python3 scripts/validate_release.py --require-publishable
python3 -m pytest
```

After this attestation merges, run the `AI-SDLC Runtime Preflight` workflow with
`candidate_mode: false`. The offline command validates the published runtime
shape without remote or credential inspection; it does not satisfy the deployed
gate.

The passing protected probe proves the repaired provider-authentication handoff.
It does not itself prove branch creation, validation, draft-PR publication,
result receipt, or source projection; those remain obligations of the fresh
end-to-end REAL verification.
