# AI-SDLC 2.4.4 release candidate

**Status:** candidate; control-plane tag and publication attestation pending.

This patch selects the reviewed `consulting-playbook` receiver repair without changing the closed v2 contract, activation, source approval, or draft-only boundary. Published `ai-sdlc-v2.4.3` at `3da7ed9b7bf76d00ae35e4accc733ac8f95259c5` is the previous known good release. The source consumer remains on 2.4.3 until the 2.4.4 publication and deployed gates have passed.

## Immutable target evidence

- `codex-adapter-v2.4.4` resolves to the reviewed target PR #63 merge commit `70ea4342abf7115f6848ea32bb958bbf6be696c1`.
- The tagged `.ai-sdlc/conformance/tc-mvp-ci-001.json` SHA256 is `63052d48e113f9420bd3d9f5e74a070b48d67a07d5ca46a0841c842ebcc6c06e`.
- The target workflow pins the future `ai-sdlc-v2.4.4` receiver. The candidate router and receiver self-pin to the same tag.
- The manifest records `tag_published: false` and `tag_commit_sha: null`; the generated runtime record says `candidate`.

## Candidate verification

```console
python3 scripts/generate_current_runtime.py --check
python3 scripts/validate_release.py
python3 scripts/validate_release.py --require-candidate-ready
python3 scripts/runtime_preflight.py --offline --candidate
python3 scripts/verify_release_target_workflows.py --repository Young-Consultations/consulting-playbook
TC_MVP_E2E_TARGET_ROOT=/path/to/immutable/consulting-playbook python3 scripts/run_tc_mvp_e2e_001.py --mode sim
python3 -m pytest
```

The target checkout for SIM must be at `70ea4342abf7115f6848ea32bb958bbf6be696c1`. Candidate readiness checks immutable adapter bindings, conformance evidence, and trusted journal authors while requiring `tag_published: false` and `tag_commit_sha: null`. The CI release check enforces this state. `validate_release.py --require-publishable` and normal Runtime Preflight must fail until publication attestation.

## Publication and deployed gates

1. Obtain protected-branch checks and maintainer/security approval on the candidate PR. Before tagging, verify the target binding, complete tests, SIM, candidate readiness validation, and verify-mode Router smoke test. Merge only when those checks pass.
2. At the reviewed candidate merge commit, repeat candidate verification and confirm that `ai-sdlc-v2.4.4` is unused. Then create the tag at that exact merge commit. Do not move an existing tag. This reviewed gate authorizes the immutable version; the tag commit cannot be recorded in the candidate before its merge.
3. In a separate attestation PR, record the exact tag commit, set `tag_published: true`, regenerate `release/current-runtime.json`, and pass `validate_release.py --require-publishable`, remote target compatibility verification, the full tests, protected-branch checks, and review. Merge the attestation before deployed gates. If this attestation fails, keep the tag fixed and the release quarantined; repair in a new reviewed PR without moving the tag.
4. Run deployed Runtime Preflight with `candidate_mode: false`, followed by immutable REAL preflight. Require both to pass before selecting a fresh harmless REAL issue and obtaining authorized human approval.
5. Only after those gates and review, update the `portfolio-tasks` source consumer to the published 2.4.4 router. Never reuse the terminal failed issue #148 for REAL acceptance.

SIM and REAL preflight do not by themselves constitute end-to-end REAL acceptance. No release tag, attestation, consumer pin change, or live approval is part of this candidate PR.
