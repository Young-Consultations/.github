# AI-SDLC 2.4.4 publication attestation

**Status:** immutable control-plane tag created at reviewed candidate merge
commit; publication attestation under review. Deployed gates remain pending.

This patch selects the reviewed `consulting-playbook` receiver repair without changing the closed v2 contract, activation, source approval, or draft-only boundary. Published `ai-sdlc-v2.4.3` at `3da7ed9b7bf76d00ae35e4accc733ac8f95259c5` is the previous known good release. The source consumer remains on 2.4.3 until the 2.4.4 publication and deployed gates have passed.

## Immutable target evidence

- `codex-adapter-v2.4.4` resolves to the reviewed target PR #63 merge commit `70ea4342abf7115f6848ea32bb958bbf6be696c1`.
- The tagged `.ai-sdlc/conformance/tc-mvp-ci-001.json` SHA256 is `63052d48e113f9420bd3d9f5e74a070b48d67a07d5ca46a0841c842ebcc6c06e`.
- The target workflow pins the future `ai-sdlc-v2.4.4` receiver. The candidate router and receiver self-pin to the same tag.
- PR #75 merged at `adb57508762168b3410f52e8a7b0151078c6e9b9`.
  `ai-sdlc-v2.4.4` resolves to that exact commit. This attestation records
  `tag_published: true` and the same `tag_commit_sha`; the generated runtime
  record says `published`.
- The existing tag is lightweight, like the prior 2.4.3 tag. The controlled
  release procedure specifies an annotated tag. Do not move or recreate this
  published tag.

**Release owner decision (2026-09-23):** Joseph explicitly approved both the
lightweight-tag variance and PR #75's lack of a formal `APPROVED` review
submission. This records acceptance of the two observed variances; it does
not claim that an annotated tag or a formal PR approval existed. The release
identity, protected checks, and post-attestation deployed gates remain required.

## Publication verification

```console
python3 scripts/generate_current_runtime.py --check
python3 scripts/validate_release.py --require-publishable
python3 scripts/runtime_preflight.py --offline
python3 scripts/verify_release_target_workflows.py --repository Young-Consultations/consulting-playbook
TC_MVP_E2E_TARGET_ROOT=/path/to/immutable/consulting-playbook python3 scripts/run_tc_mvp_e2e_001.py --mode sim
python3 -m pytest
```

The target checkout for SIM must be at `70ea4342abf7115f6848ea32bb958bbf6be696c1`.
The target verifier resolves the now-present control-plane tag remotely and
checks the immutable receiver. The offline Runtime Preflight validates local
release shape; it is not the deployed gate.

## Publication and deployed gates

1. **Completed:** merge the control-plane candidate PR #75 after candidate
   readiness, immutable SIM, and target compatibility checks passed.
2. **Completed:** create `ai-sdlc-v2.4.4` at the exact reviewed merge commit
   `adb57508762168b3410f52e8a7b0151078c6e9b9`.
3. **This attestation:** record the exact tag commit, set `tag_published: true`,
   regenerate `release/current-runtime.json`, and pass
   `validate_release.py --require-publishable`, remote target compatibility
   verification, the full tests, protected-branch checks, and review. The
   release owner has accepted the two recorded variances above. If this
   attestation fails, keep the
   tag fixed and the release quarantined; repair in a new reviewed PR.
4. After attestation merges to `main`, run deployed Runtime Preflight with
   `candidate_mode: false` from `main`, followed by immutable REAL preflight.
   The workflow rejects non-main dispatches of the deployed gate. Require both
   to pass before selecting a fresh harmless REAL issue and obtaining
   authorized human approval.
5. Only after those gates and review, update the `portfolio-tasks` source consumer to the published 2.4.4 router. Never reuse the terminal failed issue #148 for REAL acceptance.

SIM and REAL preflight do not by themselves constitute end-to-end REAL
acceptance. This PR records only publication attestation; it does not change
the source consumer pin or authorize live approval.
